<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tr.edu.yildiz.erentutus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SanalDolabim">
        <activity android:name=".AddActivity"></activity>
        <activity android:name=".UpdateActivity" />
        <activity android:name=".Activities" />
        <activity android:name=".ChooseClothesInCabin" />
        <activity android:name=".Combines" />
        <activity android:name=".CombineRoomScreen" />
        <activity android:name=".ClothesScreen" />
        <activity android:name=".DrawersScreen" />
        <activity android:name=".AddClothes" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>